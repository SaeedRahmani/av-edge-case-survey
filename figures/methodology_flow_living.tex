% AV edge-case survey update flowchart.
% Insert with \input{figures/methodology_flow_living} inside a figure environment.
\begin{tikzpicture}[
    font=\sffamily\footnotesize,
    node distance=7mm and 10mm,
    box/.style={draw, rounded corners=2pt, align=center, text width=36mm,
        minimum height=9mm, inner sep=3pt, fill=white},
    flowstep/.style={box, draw=blue!60!black, fill=blue!5},
    review/.style={box, draw=orange!75!black, fill=orange!8},
    result/.style={box, draw=green!45!black, fill=green!8},
    arrow/.style={-{Latex[length=2mm]}, thick, draw=gray!75}
]

\node[flowstep] (papers) {Papers already used\\in the manuscript};
\node[flowstep, below=of papers] (search) {Search OpenAlex\\for new related papers};
\node[flowstep, below=of search] (clean) {Clean results\\remove duplicates and unsuitable sources};
\node[flowstep, below=of clean] (check) {Check relevance\\using titles and abstracts};
\node[flowstep, below=of check] (classify) {Classify papers\\by the manuscript structure};
\node[review, below=of classify] (review) {Human review\\for uncertain papers};
\node[result, below=of review] (update) {Update living bibliography\\and repository paper list};

\node[review, right=15mm of review] (llm) {Optional LLM check\\second opinion only};
\node[flowstep, right=15mm of classify] (limit) {Monthly limit\\up to five additions};

\draw[arrow] (papers) -- (search);
\draw[arrow] (search) -- (clean);
\draw[arrow] (clean) -- (check);
\draw[arrow] (check) -- (classify);
\draw[arrow] (classify) -- (review);
\draw[arrow] (review) -- (update);
\draw[arrow] (classify) -- (limit);
\draw[arrow] (limit) -- (review);
\draw[arrow] (review) -- (llm);
\draw[arrow] (llm) -- (review);

\end{tikzpicture}